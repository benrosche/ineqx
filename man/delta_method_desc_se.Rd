% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delta_method.R
\name{delta_method_desc_se}
\alias{delta_method_desc_se}
\title{Compute delta method SEs for descriptive decomposition}
\usage{
delta_method_desc_se(
  wibe,
  totals,
  deltas,
  ystat,
  ref = NULL,
  order = "shapley"
)
}
\arguments{
\item{wibe}{Data.frame with columns: time, group, n, pi, mu, sigma, sigma2}

\item{totals}{Data.frame with columns: time, VarW, VarB, VarT, etc.}

\item{deltas}{Data.frame with delta columns, or NULL}

\item{ystat}{Character, "Var" or "CV2"}

\item{ref}{Reference time period (needed for delta SEs), or NULL}

\item{order}{Decomposition order: "shapley" or permutation of c("mu","sigma","pi")}
}
\value{
A list with:
  \item{totals}{Named list by time, each containing se_VarW, se_VarB, se_VarT
    (or se_CV2W, se_CV2B, se_CV2T)}
  \item{deltas}{Named list by time, each containing se_delta_mu, se_delta_sigma,
    se_delta_pi, se_delta_T (if deltas provided)}
}
\description{
Given group-level statistics (pi, mu, sigma, n), constructs the sampling
covariance matrix and applies the delta method to compute SEs for VarW,
VarB, VarT, and (if applicable) counterfactual deltas.
}
\keyword{internal}
