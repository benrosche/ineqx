# Registry mapping stat names to SE functions (for delta CIs)

Registry mapping stat names to SE functions (for delta CIs)

## Usage

``` r
.ineq_se_registry
```

## Format

An object of class `list` of length 5.
