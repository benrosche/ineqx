# Build outcome dist data (Control + Treated per group)

Build outcome dist data (Control + Treated per group)

## Usage

``` r
.build_outcome_dist_data(groups, mu0, sigma0, beta, lambda, trim = 0.995)
```
