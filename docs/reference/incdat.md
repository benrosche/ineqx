# Income data

A simulated income dataset for demonstrating the variance decomposition
functions.

## Usage

``` r
data(incdat)
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with
3000 rows and 7 columns.

## Examples

``` r
data(incdat)
```
