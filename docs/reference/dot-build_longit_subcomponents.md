# Build and print cross-sectional sub-component table from longitudinal results

Build and print cross-sectional sub-component table from longitudinal
results

## Usage

``` r
.build_longit_subcomponents(
  results,
  ref,
  cross_se = NULL,
  lambda_tests = NULL,
  beta_tests = NULL
)
```
