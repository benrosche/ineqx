# Build treatment effect dist data (one density per group)

Build treatment effect dist data (one density per group)

## Usage

``` r
.build_te_dist_data(groups, mu0, sigma0, beta, lambda, trim = 0.995)
```
